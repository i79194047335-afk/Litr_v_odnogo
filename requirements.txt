websockets>=12.0
requests>=2.31
pandas>=2.0
numpy>=1.26
pyyaml>=6.0
python-dotenv>=1.0
loguru>=0.7
tenacity>=8.2
# Official Lighter Python SDK (used by list_markets helper).
# Collector itself talks to the raw WS — does not need the SDK at runtime.
git+https://github.com/elliottech/lighter-python.git
