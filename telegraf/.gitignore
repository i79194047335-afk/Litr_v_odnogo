# Состояние — один номер последнего разобранного поста. Машинное, локальное:
# на новой машине первый прогон просто поставит отметку заново.
state/
logs/

# Накопленное знание (knowledge/) НЕ игнорируем — оно и есть результат работы.
