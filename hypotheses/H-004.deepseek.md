HYPOTHESIS: 
**A.** `account_type` 2 identifies a public pool operator and `account_type` 3 identifies a pool run by the venue itself. All 319 pools enumerable from `publicPoolsMetadata` carry type 2 or 3, and the two type-3 pools are `Lighter Liquidity Provider (LLP)` and `Experimental Liquidity Provider (XLP)`, both with L1 address `0x0000…0000`.
**B.** The PnL reconstructed by `src/analysis/participant_pnl.py` agrees in sign with the venue's published annual return for those pools. Of 11 pools that traded on markets 0/1/2/24 during 2026-07-28..08-02, 9 agree in sign (P = 0.033 against a coin flip); Spearman between published APR and our 5-day realised PnL is +0.52. Restricted to the 9 pools with at least 20 fills — the same threshold used elsewhere in this track — agreement is 9 of 9 (P = 0.002) and Spearman is +0.72.

VERDICT: REFUTED

GHOST-NUMBERS:
- 9/9 sign agreement (min_fills ≥ 20) — no output or log in the corpus
- P = 0.002 for the restricted subset — no output or log
- Spearman +0.7167 for the restricted subset — no output or log
- Individual pool PnL figures: LLP +22,136.72 / 12,644 fills, Edge & Hedge +7,461.94 / 3,085 fills, APR 12.73%, etc. — none appear in any committed artifact
(The numbers for the unrestricted set (9/11, +0.52) do appear in `hypotheses/H-003.result.md`.)

UNSTATED ASSUMPTIONS:
- That the baseline probability of sign agreement under independence is exactly 0.5 (coin flip). The binomial P‑value reported depends on this; the actual proportion of positive APRs among the 11 pools was never measured, making the P‑value meaningless.
- That the five‑day window’s sign of realised PnL is expected to match the sign of the annualised pool return for every pool, even though the two measures cover different time spans, markets, and include/exclude fees and flows.
- That excluding pools with fewer than 20 fills does not introduce selection bias into the sign‑agreement metric (the hypothesis publishes both, but the restricted set is presented as stronger evidence without a proper independence null).

ALTERNATIVE EXPLANATIONS:
- The sign agreement could occur simply because most public‑pool operators are market‑makers (many report 100% maker share) and market‑making is usually profitable in both short and annual horizons; thus a noisy PnL reconstruction that merely gets the maker/taker direction right will often show the same sign as the annual APR, even if the arithmetic is incorrect.
- Rank correlation of +0.52 over 11 points is not statistically distinguishable from zero; with such small n, a single outlier can drive the coefficient, and no significance level was computed.
- The restricted subset (9 pools) achieves perfect agreement because the two excluded pools had very few fills and their noisy 5‑day PnL happened to disagree with the APR; removing them is a form of post‑hoc filtering that inflates the agreement rate.

ARITHMETIC: The binomial tail for 9 out of 11 under a fair coin is 67/2048 ≈ 0.0327, matching the reported 0.033. The Spearman calculations themselves cannot be recomputed without the underlying PnL and APR vectors, but the formula is standard. The claim’s statistical inference, however, is built on an unverified assumption (see above).

CONFLICTS WITH RECORD:
- `hypotheses/H-003.result.md` contains only the 9/11 agreement and +0.52 Spearman; it does not mention the restricted 20‑fill analysis or the 9/9 agreement and +0.72, contradicting the hypothesis’s assertion that those numbers come from “same tool, `--min-fills 20`” and are “measured”.
- The per‑pool figures (LLP, Edge & Hedge) are absent from every file in the corpus except the hypothesis itself—they are asserted, not measured in the submitted record.

KILL-SHOT:
- Measure the proportion of positive APRs among the 11 pools (or, more practically, among all 319 pools enumerated by `src/analysis/pool_validation.py`). Then recompute the binomial P‑value for sign agreement using that proportion as the success probability under the null of independence. If the corrected P‑value exceeds 0.05, the claimed statistical significance vanishes, directly refuting the hypothesis as written. The command is:  
  `python -m src.analysis.pool_validation --cache pools.json` (to fetch pool metadata), then extract `annual_percentage_yield` signs, count the positive fraction, and apply the binomial test.  
  This does not require the tape data—only the already‑enumerable pool metadata.
