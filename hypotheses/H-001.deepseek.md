HYPOTHESIS: `swing_confirm_bars = 2` is the optimal value for the swing tracker. Widening it was tested on real data and made backtest results worse, so the parameter does not need to be swept and 2 can be kept as the fixed default in all further work.

VERDICT: UNTESTABLE

GHOST-NUMBERS:
  - Widened swing_confirm_bars performance (any net bps / t-stat) — no source found; only prose assertions in `swings.py` docstring, `CONTEXT.md`, and `ACCEPTANCE_SWEEP_2026-07-10.md`

UNSTATED ASSUMPTIONS:
  - The informal test of widened swing_confirm_bars was run under exactly the same configuration (costs, fill probability, exit mode, range_size, data days) as the current baseline, and the result would replicate.
  - "Worse" implies no other value could be better; the effect is monotonic around 2.
  - The outcome of that test was recorded and is trustworthy despite the absence of any numeric record.

ALTERNATIVE EXPLANATIONS:
  - The reported worsening could be an artefact of a bug, a single outlier day, or an interaction with `acceptance_bars` / trailing that was not isolated.
  - The current baseline at `swing_confirm_bars=2` is already negative and statistically indistinguishable from noise; a wider setting might produce a similar or even less negative result and was misclassified as "worse" due to insufficient testing.

ARITHMETIC: No original arithmetic is present — the hypothesis only cites existing measured numbers from the `acceptance_bars` sweep, which are correctly quoted.

CONFLICTS WITH RECORD: Yes. The hypothesis claims "widening it was tested on real data and made backtest results worse." The project record explicitly states the opposite: `swing_confirm_bars` was **NEVER swept** and the claim appears only as prose with no numbers recorded anywhere (`CONTEXT.md`, session 2026-07-24, and `AUDIT_2026-07-10.md` item S2). The premise of prior measurement is false.

KILL-SHOT: Run `python -m src.backtest.run_backtest --swing-confirm-bars N` for N=2,3,4,5 on the untouched holdout days (2026-07-07 through 2026-07-09) using the same settings as the acceptance-sweep baseline (exit_mode=swing, trailing=False, costs zero, fixed range_size=15.3 or rolling calibration). Compute net bps per setting. If any N > 2 yields a higher (less negative) net bps than the N=2 result, the hypothesis that 2 is optimal is killed.
