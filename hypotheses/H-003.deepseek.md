HYPOTHESIS: A. Realised PnL per account can be reconstructed from the public `trade/{market}` WS feed alone. The position arithmetic it rests on predicts the venue's own next reported `*_position_size_before` and `*_entry_quote_before` for 99.956%–99.995% of checks across 4 markets on 2 days. B. That reconstruction does **not** by itself identify persistently profitable participants on a per-day basis. Overlap of the top-20 by realised PnL between two consecutive days is 4/20 on market 24 and 5/20 on market 2, against a random-draw baseline whose 95th percentile is 5 on both. On market 0 the overlap is 8/20 against a baseline p95 of 3.

VERDICT: UNTESTABLE

GHOST-NUMBERS: ["4/20 (m24 top-20 overlap)", "5/20 (m2 top-20 overlap)", "8/20 (m0 top-20 overlap)", "mean 2.79 / 2.62 / 1.03 (random-draw baseline)", "p95 5 / 5 / 3 (random-draw baseline)", "-11,679 (m24 20260730 summed PnL)", "+41,378 (m24 20260731 summed PnL)", "+91,057 (venue positions sum)", "-1,216 (flat accounts PnL)"]

UNSTATED ASSUMPTIONS:
  - That gross (fee-excluded) realised PnL captured by the reconstruction is the appropriate ranking metric for identifying persistently profitable participants, despite documented uncertainty about the fee unit and the documented possibility that net PnL could change rankings (API_DIGEST.md).
  - That a single day’s realised PnL (a realisation of a very short horizon) and day‑to‑day top‑20 overlap are sufficient to judge whether the reconstruction identifies persistently profitable participants; a longer accumulation period or a different threshold might reveal persistence.
  - That the random-draw baseline (independent draws of 20 accounts, ignoring PnL distribution) is the correct null model for the overlap, ignoring any structural autocorrelation in PnL that might arise from trading style even without genuine skill.

ALTERNATIVE EXPLANATIONS:
  - The low overlap could reflect that actual profitability is near‑random on a daily timescale, not that the reconstruction fails to capture it; the reconstruction itself may be sound, but there is simply no persistent signal to identify.
  - If the reconstruction were gross of fees, accounts whose true net profitability comes mainly from fee rebates (e.g., pure makers) would be misranked or lost entirely, leading to an artificially low overlap.
  - The market‑0 result (8/20, above the random‑draw p95 of 3) already suggests that for at least one market the reconstruction shows some persistence; this contradicts the blanket statement “does **not** by itself identify …”, so the hypothesis could be refuted by its own data if the numbers were verified.

ARITHMETIC: Cannot be verified — the key numbers (top‑20 overlaps, random baseline, summed PnL, venue position sums) are asserted without any output log or reproducible artifact in the project record, so no independent recomputation is possible.

CONFLICTS WITH RECORD: The numbers for top‑20 overlap and all derived quantities (baseline, PnL sums) have no traceable source in the project record; they are absent from `LOG.md`, from any artifact file, and from the tool outputs. The hypothesis thus rests on unobservable claimed measurements. Additionally, `API_DIGEST.md` documents that the fee unit is unestablished and that gross PnL ranking may differ from net, directly undermining any conclusion drawn from a gross‑PnL ranking.

KILL-SHOT: Use the existing tool to obtain the realised PnL of every eligible account on days 20260730 and 20260731 for market 24 (`python -m src.analysis.participant_pnl --market 24 --days 20260730 20260731 --top 0 --min-fills 20`). Compute the Spearman rank correlation between the two days’ PnL values. If the correlation is positive and statistically significant (p < 0.05), the assertion that the reconstruction does **not** identify persistently profitable participants is refuted. Alternatively, compute the intersection of the top‑20 accounts across three consecutive days (e.g., 20260729‑20260731); a non‑empty intersection (or one exceeding the random‑draw baseline for three independent draws) would refute the blanket negative claim.
