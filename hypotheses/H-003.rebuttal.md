AGREE-WITH:
  - He correctly isolates the ghost baseline (the p95 values from an unreproducible scratch script) and acknowledges the contamination in his original overlap numbers from the `min_fills` selection artifact.
  - He affirms that Claim A’s arithmetic is sound and supported by the `pnl_probe` output logged in `LOG.md`, and that account-id persistence is established.
  - He notes the unresolved fee unit and the need for longer windows — both essential limitations.

UNSUPPORTED-CLAIMS:
  - “Re-running with both rankings restricted to accounts eligible on *both* days: 8/20 (market 24), 5/20 (market 2), 9/20 (market 0)”.  No traceable source exists in the project record; the file `hypotheses/H-003.md` contains only the original uncorrected overlaps, and no output artifact of this re-run is committed anywhere.
  - The sign‑agreement figures: “market 24: 71/147 agree = 48.3%”, “market 2: 68/152 agree = 44.7%”, “market 0: 201/403 agree = 49.9%”.  They appear nowhere in `LOG.md`, in any tool‑output file, or in any committed script log.  The same applies to the inference that “PnL sign carries no information”.
  - The corrected numbers are presented as the “honest version” and “stronger and worse for the track,” yet their provenance is indistinguishable from the ghost numbers he criticises — they are asserted, not traced to a measurable artifact.
  - The original baseline p95 values (5/5/3) are called ghosts, but the corrected overlaps are still implicitly compared to them (or to an ideal random expectation) without a traceable replacement; the statement “market 24 doubles” has no anchored benchmark.
  - “median implied maker fee … is not negligible” — the size of that fee is never stated, let alone sourced; the claim remains a qualitative hand‑wave.

MISSED:
  - The impact of the `min_fills=20` threshold on the sign‑agreement test: accounts with fewer fills are excluded, which may systematically remove low‑frequency but persistently profitable participants and bias the agreement rate.
  - The 99.99% reconstruction accuracy may be driven by the bulk of simple, flat‑to‑flat position sequences; the remaining residue could concentrate on the very accounts that would appear at the top of a profit ranking (e.g., those with many flips or sign changes), yet the verdict does not check whether the top‑20 or top‑earners ever appear among the probe’s failure cases.
  - The point‑farming operation (33% of accounts by count, all 100% taker) is never mentioned in connection with B.  Those accounts could produce random‑looking PnL noise that masks genuine persistence, and their omission from the population would change the measured overlap.

DISPUTED:
  No factual dispute is listed because every new figure in the verdict is itself unverifiable from the record.  Asserting that “Market 2 is unchanged” or that the corrected overlap is 5/20 remains uncheckable.

CITATIONS-TO-VERIFY:
  - `hypotheses/H-003.md` (entire file) → contains only the claimed overlap 4/20, 5/20, 8/20 and the ghost baseline 5/5/3; no corrected numbers or sign‑agreement data appear.
  - `LOG.md` (entire file) → records the `pnl_probe` pass rates and persistence result; does not contain any of the corrected overlap values, sign‑agreement rates, or the re‑run method.
  - `src/analysis/participant_pnl.py` → is the tool that could produce per‑day PnL, but no run output is committed for the re‑run; hence the new numbers are not reproducible from the record.
  - `docs/API_DIGEST.md` (section “maker_fee / taker_fee — единица НЕ УСТАНОВЛЕНА”) → confirms that the fee unit is unknown, so any net‑vs‑gross distinction is speculative.

NEW-KILL-SHOT: none - the round-1 kill-shot stands
