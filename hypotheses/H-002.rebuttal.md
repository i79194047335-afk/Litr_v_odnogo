AGREE-WITH: The account-type classification (0/1/2/3 with exact id-range correspondence) is correctly traced to the venue’s own `/account` response, and the zero-address account is indeed type 3.  That the word “few” misrepresents the mid-range count (more accounts by count) also holds — the hypothesis’s prose is imprecise.  Those points are load-bearing because they separate what can be accepted at face value from what cannot.

UNSUPPORTED-CLAIMS:
  - The daily breakdown of the zero-address account’s share (0.94% Jul 29, 0.86% Jul 30, 1.99% Jul 31) — no output file, day‑by‑day run log, or mention in H-002.md provides these figures; they are asserted.
  - The per‑day role‑split numbers across three BTC days (68.5% / 82.7% / 76.8%) — the hypothesis file contains only the single 76.8% spot, and no artefact in the PROJECT RECORD records the other two.
  - The HYPE‑market role split (45.7% pure share, 40.4% mid‑range) — no report output or summary is present for market 24, and H-002.md’s evidence table stops at liquidation rates for that market.
  - The count of 392 mid‑range accounts on BTC Jul 31 and the 18.3% volume share — the `report_participants.py` script does not print a mid‑range summary, and no run output was supplied; the number is calculated outside the record.

MISSED:
  - The hypothesis’s central claim that “participants do not switch” is a statement about persistence over time, yet the verdict only examines a one‑day snapshot.  No cross‑day account‑identity tracking was done, and the verdict never checks whether the same accounts keep their pure‑maker/pure‑taker label from day to day.
  - The 100‑fill threshold is an unstated assumption that can inflate the appearance of bimodality — accounts that switch roles more often may fall below the cutoff and disappear from the “accounts with ≥100 fills” view exactly when they are most interesting.  The verdict does not examine how the distribution changes if the threshold is lowered or removed.

DISPUTED:
  - The statement that “the role‑split half survives … true on BTC as stated” is an over‑acceptance.  The hypothesis asserts that participants **do not switch**, which is a behavioral claim that requires evidence of across‑day stability, not just a single‑day classification.  The one‑day figures (even if accurate) do not disprove switching — they only show that on that day high‑purity accounts existed.  Our round‑1 verdict refuted the claim on those grounds, and the evidence the verdict supplies does not settle the question.  Settling would require the cross‑day measurement proposed in the round‑1 kill‑shot.

CITATIONS-TO-VERIFY:
  - `hypotheses/H-002.md` -> contains the Jul 31 numbers (98 pure makers 41.4%, 431 pure takers 35.4%) and states “participants do not switch … with few accounts in between”; also documents the account‑type counts and the zero‑address observation.
  - `src/analysis/report_participants.py` -> produces per‑day role‑purity counts and volume shares for a single market/day; it does not output mid‑range summaries or cross‑day comparisons, so the 392 mid‑range figure cannot be read from its normal output.
  - `src/analysis/participant_stats.py` -> defines the per‑account maker/taker aggregation but contains no persistence logic — it treats each day independently and cannot answer whether the same account appears in a different role class on a different day.
  - `tests/test_participant_stats.py` -> tests the arithmetic of the aggregation (over a single stream of fills), not the correctness of the “do not switch” inference.

NEW-KILL-SHOT: none — the round‑1 kill‑shot stands.  Unchanged: run `report_participants` for both 20260730 and 20260731 (both closed BTC days with the same ≥100‑fill rule) and check whether any individual account has maker share ≥0.95 on one day and ≤0.05 on the other.  A single such flip kills the claim that those participants “do not switch,” and the verdict under attack never attempts this cross‑day comparison.
