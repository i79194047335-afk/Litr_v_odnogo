HYPOTHESIS: 
**A.** On the day-pair 31 Jul → 1 Aug 2026, restricted to accounts that cleared 50 fills on **every** day 29 Jul – 2 Aug, daily realised-PnL rank correlation stands above its own permutation null band on all three markets measured (0, 2, 24) — and this is not an artefact of the restriction, because the same restriction leaves the other three adjacent pairs inside the null band or negative.
**B.** On market 0 the effect is a *rank* effect and not a *magnitude* effect: Spearman +0.581 against Pearson +0.023 over the same 143 accounts and the same two days. Therefore the observation cannot size a position, only order accounts.
**C.** Daily-PnL ranking is rejected as a selection mechanism for this track. Of four adjacent pairs on complete days, three sit in the null band on every market; the fourth is the pair above.

VERDICT: NEEDS-DATA

GHOST-NUMBERS:
  - all full‑population ρ values (pairwise Spearman rhos, 4 pairs × 3 markets): no source script or data log found in the supplied project record; the claimed script `src/analysis/pnl_persistence.py` is absent.
  - all core‑restricted ρ values and their permutation‑null p95 bands: same absence.
  - core sizes 143 / 56 / 53: no script output or data file present.
  - Spearman +0.581, Pearson +0.023 (m0, core): no artefact from the claimed ad‑hoc script.
  - top‑quintile retention 19/28, 6/11, 6/10: ghost.
  - m0 top‑8 31 Jul, two losses −8 886 and −14 400 on 1 Aug: ghost.
  - 1 Aug tape covers 24.0 h: the hypothesis names `trades_full_*_20260801.jsonl.gz` – that file is not in the record, and the record’s tape collection snapshot predates that day.
  - daily volume m24 $37.3M→$18.0M, m0 $287M→$102M: ghost.
  The hypothesis file itself asserts “measured” for every number, but the project record contains **no trace** of the named scripts, their outputs, or any raw data for the five days used.

UNSTATED ASSUMPTIONS:
  - That the five‑day fill filter (≥50 fills every day) selects a stable set of accounts whose daily PnL ranks are comparable across days; no analysis of survivorship or account‑identity persistence is provided, yet account‑index stability is a known open question in the project record.
  - That the permutation null for the core‑restricted set is constructed correctly (e.g., the restriction itself does not alter the null distribution by conditioning on the selection). The hypothesis questions this but does not resolve it.
  - That daily realised PnL can be reconstructed from tape data without systematic bias, despite the record’s explicit warning that this derivation has its own error surface and that ready‑made PnL fields do not exist on the wire (the `*_pnl` fields are absent).
  - That the three markets (0,2,24) provide independent tests; the record itself warns that they share accounts and therefore are not independent.

ALTERNATIVE EXPLANATIONS:
  - The rank correlation on 31 Jul → 1 Aug could be caused by a large common market move that affected most accounts in the same direction, creating high rank agreement without any skill persistence – i.e., a single‑day event, not a replicable signal.
  - The selection of core accounts by fills on the very five days that include the test pair may induce correlation by retaining only the most active accounts, which on volatile days (31 Jul → 1 Aug) might have correlated PnL simply because they remained active and took the same side of a market event.
  - The Spearman/Pearson gap could be produced by a few extreme‑outlier accounts that dominate the Pearson but are dampened in rank; the hypothesis’s own numbers show two accounts losing large absolute amounts, which would heavily influence a mean‑based measure while barely moving ranks.

ARITHMETIC: Cannot independently recompute. Every numerical result relies on data and scripts that are absent from the supplied project record. No independent verification of the core‑restricted ρ, the Spearman gap, or the null bands is possible from the provided corpus.

CONFLICTS WITH RECORD:
  - The project record (`START_participant_mining.md`, session 2026‑07‑24) explicitly states that realised PnL must be reconstructed from position‑before fields and flags, that this reconstruction has “an error surface of its own,” and that **ready‑made `*_pnl` fields are absent from the wire**. The hypothesis’s entire PnL‑ranking rests on a reconstruction whose methodology and validation are nowhere in the record; the record itself would treat that reconstruction as a prime target for falsification, yet H‑006 offers no verification of its correctness.
  - The record notes that account‑index persistence over time is an open blocking question (session 2026‑07‑24). If account indices rotate, the core‑restricted set might not represent the same entities across the five days, invalidating both the restriction and the rank comparison.

KILL-SHOT:
Run the same analysis (`--min-fills 50`) but define the core accounts using fills on **29, 30, and 31 July only** (i.e., exclude the day of the tested pair 1 Aug and the following day). Then compute the rank correlation ρ for the pair 31 Jul → 1 Aug on that core. If the correlation falls inside the null band computed for the alternative core, the original result is an artefact of conditioning on fills that overlap with the test pair. Command sketch:
```
python src/analysis/pnl_persistence.py --min-fills 50 --core-days 20260729,20260730,20260731 --test-pair 20260731,20260801
```
with the null band recalculated for the new core. A drop below the band kills the claim.
