AGREE-WITH:
The identification of the prose-only "widening made results worse" as a ghost number is correct and load‑bearing — no numeric sweep result exists. The distinction between the measured acceptance_bars baseline numbers (which are real but say nothing about swing_confirm_bars) and the unmeasured claim is essential.

UNSUPPORTED-CLAIMS:
  - "No results table anywhere in the repo has swing_confirm_bars as a column" — the verdict states this as a fact without providing the search artifacts (grep / ls output) that would prove the negative across all branches and commits. The assertion is plausible, but the kill‑shot’s own search commands are not shown, so the claim is unverified.  
  - "the kill‑shot (2) … on the fixed day split from docs/PASS_FAIL_CRITERION.md" — the cited document’s banner explicitly states that its out‑of‑sample split is wrong and the document is suspended; it does NOT define a trustworthy fixed split. Therefore the claim that the split is usable as a measurement basis is contradicted by the record and unsupported.  
  - "costs as configured rather than zero" — the project has no configuration file that specifies a cost model for a swing_confirm_bars sweep; the baseline measurements were run with zero costs. The phrase “as configured” is ambiguous and no such configuration is traceable.

MISSED:
  - Both verdicts ignore the interaction with rolling range_size calibration. The sweep proposed by Claude (and the earlier round‑1 kill‑shot) would fix range_size at the uncalibrated constant 15.3, whereas the project has moved to rolling daily recalibration (AUDIT A4). A sweep under a fixed bar size does not answer whether N=2 is optimal under the live‑capable rolling‑sizing regime.  
  - Neither verdict considers that the untouched holdout days (2026‑07‑07 to 09) already sit on disk; the PASS_FAIL_CRITERION.md split is suspended, but a valid holdout exists and could be used without waiting for a new split decision. This makes the verdict “UNTESTABLE” (from round‑1) too cautious in practice, but the record was not examined for that availability.

DISPUTED:
  - CLAIM: Claude’s verdict asserts that the hypothesis is REFUTED because limb (a) (the alleged prior test) is false and the conclusion does not follow from a negative baseline.  
    COUNTER: The core claim “swing_confirm_bars=2 is optimal” has never been compared to any other value; the falsehood of a supporting limb does not disprove the optimality claim itself. The record offers no measurement that shows another value is better, so the hypothesis is not demonstrably false. The appropriate label, given the absence of a defined metric and sweep, is “UNTESTABLE” — not “REFUTED”.  
    SETTLE: Run a sweep of swing_confirm_bars over N = 1..5 on the untouched holdout days (2026‑07‑07 to 2026‑07‑09) with the rolling‑size calibration and the current exit_mode=swing, trailing=False, costs zero (or the project’s actual cost model if later defined). Report net bps per value. If any N other than 2 yields a higher net bps (or the series is non‑monotone and 2 is not the peak), the hypothesis would be refuted. If 2 remains the best, the hypothesis is not refuted (though it may still be untestable without a cost/split definition, the measurement would provide evidence). This sweep would discriminate between the two verdict labels.

CITATIONS-TO-VERIFY:
  - `docs/PASS_FAIL_CRITERION.md` banner → “out‑of‑sample” split (Jul 3‑6) is no longer out‑of‑sample, and the document is suspended. This refutes the existence of a fixed day split usable for the proposed kill‑shot (2).  
  - `docs/AUDIT_2026-07-10.md` item A2 → states that Jul 7‑9 are untouched holdout days and recommends a calibration/validation/holdout split; this shows that the only clean holdout is not the suspended split.  
  - `docs/ROLLING_CALIBRATION_2026-07-10.md` → rolling prior‑day range_size is in place; any sweep using a constant range_size=15.3 fails to account for the adaptive sizing that now characterizes the project, so the kill‑shot (2) is also underspecified.  
  - `hypotheses/H-001.result.md` (implied from the project record’s session log of 2026‑07‑28) → records that the protocol shakedown already produced a refutation of the ghost and that the kill‑shot (search for sweep artifacts) was executed, confirming the absence; this supports that the unsupported absent‑table claim is plausible but still needs its search output for full provenance.

NEW-KILL-SHOT: Sweep `swing_confirm_bars` over N = 1,2,3,4,5 on the untouched holdout days 2026‑07‑07 through 2026‑07‑09 using rolling range_size (from prior day) and the current swing‑mode, trailing=False, costs zero. Report net bps and t‑stat per N. If any N > 2 or N = 1 yields a net bps that is higher than (or statistically indistinguishable from) N = 2, the hypothesis “2 is optimal” is dead; if N = 2 remains the best, the claim of optimality cannot be dismissed as untestable. This directly discriminates between the “REFUTED” and “UNTESTABLE” stances.
