# Раздутый рабочий кэш: 100+ страниц документации, перекачиваются при первом запуске.
state/snapshots/
logs/

# Состояние — машинное, локальное, восстанавливается прогоном.
state/pages.json

# Накопленное знание (knowledge/) НЕ игнорируем — оно и есть результат работы.
